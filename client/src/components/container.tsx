import Container from '@mui/material/Container';
import React, { useContext } from 'react';
import { MainContext } from '../context/maincontext';


export default function Containerx() {

    // const {region} = useContext(MainContext);

    return (
        <Container fixed>
            
        </Container>
    );
}